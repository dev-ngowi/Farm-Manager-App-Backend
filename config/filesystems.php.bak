<?php

return [

    'default' => env('FILESYSTEM_DISK', 'local'),

    /*
    |--------------------------------------------------------------------------
    | Filesystem Disks
    |--------------------------------------------------------------------------
    */
    'disks' => [

        'local' => [
            'driver' => 'local',
            'root' => storage_path('app'),
            'throw' => false,
        ],

        'public' => [
            'driver' => 'local',
            'root' => storage_path('app/public'),
            'url' => env('APP_URL').'/storage',
            'visibility' => 'public',
            'throw' => false,
        ],

        'files' => [                   // ← this is the one that was breaking
            'driver' => 'local',
            'root' => storage_path('app/files'),
            'visibility' => 'private',
            'throw' => false,
        ],

        // add others if needed (s3, etc.)
    ],

    'links' => [
        public_path('storage') => storage_path('app/public'),
    ],

];
